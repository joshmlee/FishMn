# TODO

## When setting up a custom domain
- Remove the redirect `index.html` from the repo root (the one that redirects `joshmlee.github.io` → `joshmlee.github.io/FishMn`)
- Point the custom domain at GitHub Pages (Settings → Pages → Custom domain)
- Update the AdSense site URL to the new domain
